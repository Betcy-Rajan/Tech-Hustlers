enum TextSizes {
  small,
  medium,
  large,
  extraLarge,
}

enum OrderStatus {

  processing,
  shipped,
  delivered,
 
}
enum PaymentMethods {
  creditCard,
  debitCard,
  paypal,
  bankTransfer,
}